{{ .Chart.Name }} dashboard artifacts have been installed.

Dashboard ConfigMaps are rendered into:
  {{ include "rancher-monitoring.dashboardNamespace" . }}

Cluster-level integration metadata is published in:
  {{ include "rancher-monitoring.fullname" . }}-dashboard-values
  namespace: {{ include "rancher-monitoring.namespace" . }}

Set `dashboardIntegration.grafanaURL` to your external Grafana base URL.
Optionally set `dashboardIntegration.prometheusURL` for consumers that need a Prometheus endpoint hint.
Optionally set `dashboardIntegration.alertmanagerURL` for consumers that need an Alertmanager endpoint hint.

For Rancher UI links and embedded dashboards to work, set the IP address of each
external service so that proxy Services and Endpoints are created in-cluster:
  dashboardIntegration.grafana.host      - IP address of external Grafana
  dashboardIntegration.prometheus.host   - IP address of external Prometheus
  dashboardIntegration.alertmanager.host - IP address of external Alertmanager
